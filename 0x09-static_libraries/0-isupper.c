#include "main.h"

/**
 * _isupper - check if upper case letters
 * @c: char to check
 *
 * Return: if uppercase return 1, else return o
 */

int _isupper(int c)
{
	if (b >= 'A' && b <= 'Z')
		return (1);
	return (0);
}
